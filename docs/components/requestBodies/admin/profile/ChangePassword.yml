content:
  application/json:
    schema:
      required:
        - current_password
        - password
        - password_confirmation
      allOf:
        - $ref: ../../../fields/currentPassword.yml
        - $ref: ../../../fields/password.yml
        - $ref: ../../../fields/passwordConfirmation.yml
